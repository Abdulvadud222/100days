import time
# from turtle import Turtle, Screen
#
# yo = Turtle()
# yo.shape("square")
# yo.penup()
# vo = Turtle()
# vo.shape("square")
# vo.penup()
# vo.goto(x=-20, y=0)
# screen = Screen()
# screen.tracer(0) ###whenever I use "screen.update()", must I use it!
#
# for i in range(30):
#     screen.update() ###It refreshes the screen after "time.sleep". So whenever I use update(), must I use "time.sleep()"
#     time.sleep(0.05) ###This controls how  fast the turtle moves. Choose a number smaller than 1.
#     yo.forward(10)
#     vo.forward(10)
#
#
#
# screen.exitonclick()